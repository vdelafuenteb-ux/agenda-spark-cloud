rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // progress-attachments and note-images buckets: authenticated reads/writes.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
